export type TaskProps = {
    task_id: number
    title: string
    description: string
    assigned_to: string
    project_id: string
    due_date: string

    //status: string
    //priority: string
}